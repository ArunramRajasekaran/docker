apiVersion: eksctl.io/v1aplpha5
kind: ClusterConfig

metadata:
  name: expense
  region: us-east-1

managedNodeGroups:
  - name: expense-ng
    instanceTypes: [m5.large, t3.micro, t2.micro]
    desiredCapacity: 3
    spot: true